# generate.sh がこのファイル名の `.env` を読み込む（.gitignore 済み）。
# GitHub Actions の secrets と同じ値を入れる。

# Google AI Studio の API キー。記事本文の生成に使う。
GEMINI_API_KEY=

# 公開通知・失敗通知の送り先。空にすると通知は一切出ない。
DISCORD_WEBHOOK_URL=
